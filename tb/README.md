# tb/

Testbenches for the modules in [../rtl](../rtl). One testbench per RTL module, named
`<module>_tb.v` (e.g. `fsm_tb.v` for `rtl/fsm.v`).

Simulation status for each testbench is tracked in [../README_SIM.md](../README_SIM.md).
